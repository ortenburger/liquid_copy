{
  "name": "liquid-copy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "fast-check": "^4.1.1",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
